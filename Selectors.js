// Selectors - Interagem com conteúdo da VIEW

// DOMContentLoaded = Executar Seleção APÒS a página estar pronta
document.addEventListener("DOMContentLoaded", function() {
    // Selector by ID do HTML
    let element = document.getElementById("element");
    console.log(element);
});

// Podemos fazer document.getElement por outras categorias
// ByClasssName, ByTagName